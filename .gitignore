# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Environnement virtuel
.venv/
venv/
env/

# Fichiers de config locaux
.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Historique d'évaluation (append-only, local)
eval_history.jsonl

# Artefacts d'évaluation DeepEval (local, hors runtime)
.deepeval/

# Index ChromaDB — artefact RUNTIME volumineux (~37 Mo) modifié à chaque accès :
# reste sur disque, jamais versionné (reconstruisible via make ingest ;
# volume Docker bind-mount en phase 6).
chroma_db/

# Sortie intermediaire de l'extraction PDF (crops de figures GLM-OCR)
_glmocr_json/

# Artefacts LaTeX (le rapport vit désormais dans un dépôt séparé)
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.bbl
*.blg
